{
  "name": "@ui5-editor-tools/test-utils",
  "version": "0.0.1",
  "private": true,
  "description": "Test Utils",
  "files": [
    "resources",
    "lib/src",
    "api.d.ts",
    "src"
  ],
  "main": "lib/src/api.js",
  "repository": "https://github.com/sap/ui5-editor-tools/",
  "license": "Apache-2.0",
  "typings": "./api.d.ts",
  "dependencies": {
    "@ui5-editor-tools/semantic-model": "0.0.1"
  },
  "devDependencies": {},
  "scripts": {
    "ci": "npm-run-all clean compile lint",
    "clean": "rimraf ./lib ./coverage ./nyc_output",
    "compile": "yarn run clean && tsc -p .",
    "compile:watch": "tsc -p . --watch",
    "lint": "eslint . --ext .ts --max-warnings=0 --ignore-path=../../.gitignore"
  }
}
